#include "main.h"

void print_diagsums(int *a, int size)
	{
        int i, j;

        for (i = 0; i < size; i++)
        {
		for (j = 0; i < size; j++)
                {
			if (i == j)
			{
				a[i];
				printf("%ls ", a);
			}
		}
	}
        
}
