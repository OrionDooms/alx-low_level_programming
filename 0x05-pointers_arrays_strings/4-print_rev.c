#include "main.h"

void print_rev(char *s)
{
	char x;
	int i, a, b;

	i = 0;
	a = 0;
	b = 0;

	while (s[i] != '\0')
	
		i++;
	}

	a = i - 1 ;
	while (b < i)
	{
		x[b] = s[a];
		_putchar(x[b]);
		a--;
	}
}
