#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first test case
 * @b: the second test case
 * Return: the result of the two multiplied integer
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
